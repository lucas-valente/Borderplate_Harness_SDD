---
name: valide
description: "Validate the builds of all project apps."
---

Validate the builds of all apps in the project using `npm run build` (or the project-appropriate build command). If all builds pass, return `builds validados com sucesso`.
